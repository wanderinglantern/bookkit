"""Pipeline tab: opportunities, submissions out. Empty for now — a home for
whichever later task builds them, split out alongside routes/relationship.py
and routes/work.py so tab modules land in disjoint files (Task 8)."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()
